// script.js
// Tambahkan logika interaktif di sini jika diperlukan

// Contoh interaksi sederhana: scroll ke atas saat halaman dimuat
window.addEventListener('load', () => {
  window.scrollTo(0, 0);
});
